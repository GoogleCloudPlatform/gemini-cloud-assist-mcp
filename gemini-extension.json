{
  "name": "GeminiCloudAssist",
  "version": "0.1.0",
  "mcpServers": {
    "gemini-cloud-assist-mcp": {
      "command": "npm",
      "args": ["run", "start:fresh"],
      "cwd": "${extensionPath}",
      "timeout": 600000
    }
  }
}
